关于cpp中的随机数生成
这里可没有python的torch那么好用的东西，作为代替有一个库<random>(c++11)
#include <random>

int main(){
    random_device rd;//这一步是基于设备提供一个真正的随机种子
    mt19937 gen(rd()); //基于梅森旋转算法，生成大量伪随机数
    uniform_int_distribution<> dist(10,20) //把生成的随机数映射到(10,20)区间(如果想是double,int)改real就可以


}